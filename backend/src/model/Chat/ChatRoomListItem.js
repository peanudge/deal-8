export default class ChatRoomListItem {
  constructor(roomId, username, thumbnail, content) {
    this.roomId = roomId;
    this.username = username;
    this.thumbnail = thumbnail;
    this.content = content;
  }
}
